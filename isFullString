public static boolean isFullString(String word) {
    for(int i = 0; i < word.length(); i++) {
        int index = word.charAt(i);
        if(index >= 48 && index <= 57) return false;
    }
    return true;
}
